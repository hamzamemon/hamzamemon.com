import { css } from 'styled-components';

const variables = css`
  :root {
    --black: #000000;
    --pink: #ffa0c5;
    --white: #ffffff;
    --yellow: #ffff00;

    --font: 'ActionMan', -apple-system, system-ui, sans-serif;
  }
`;

export default variables;
